description = "List in-progress dev-flow tasks with phase, next action, and blockers"
prompt = """
Invoke the dev-orchestrator skill — task dashboard mode (dev-flow-tasks).

Resolve paths using docs/dev-orchestrator-paths.md. Summarize active and
paused tasks per the skill's Task Dashboard section.
"""
